<style>
  /* 폰트 : 인천교육자람체 */
  @font-face {
    font-family: "iceJaram-Rg";
    src: url("https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_2307-2@1.0/iceJaram-Rg.woff2")
      format("woff2");
    font-weight: normal;
    font-style: normal;
  }
  /* 폰트 : 프리텐다드 */
  @import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css");

  body {
    background-size: cover;
    background-color: #fefff7;
  }

  /* 홈페이지 좌우 여백 조절하는 곳 */
  .cover-container {
    max-width: 72em;
  }

  /*
   * Header
   */

  .cover-container .d-flex .w-100 .h-100 .p-3 .mx-auto .flex-column {
    text-align: center;
  }

  /* 헤더 로고 폰트 */
  .logo {
    font-family: "Pretendard";
    font-size: 39px;
    font-weight: 900;
  }

  /* 헤더 메뉴 폰트&사이즈 (굵기는 아래 html에서 fw-값을 바꿔야함.) */
  nav {
    font-family: "Pretendard";
    font-size: 18px;
  }

  .nav-masthead .nav-link {
    color: #6a6a6a;
    border-bottom: 0.25rem solid transparent;
  }

  .nav-masthead .nav-link:hover,
  .nav-masthead .nav-link:focus {
    color: #818181;
  }

  .nav-masthead .nav-link + .nav-link {
    margin-left: 1rem;
  }

  .nav-masthead .active {
    color: #818181;
    border-bottom-color: #818181;
  }

  .bd-placeholder-img {
    font-size: 1.125rem;
    text-anchor: middle;
    -webkit-user-select: none;
    -moz-user-select: none;
    user-select: none;
  }

  @media (min-width: 768px) {
    .bd-placeholder-img-lg {
      font-size: 3.5rem;
    }
  }

  .b-example-divider {
    width: 100%;
    height: 3rem;
    background-color: rgba(0, 0, 0, 0.1);
    border: solid rgba(0, 0, 0, 0.15);
    border-width: 1px 0;
    box-shadow: inset 0 0.5em 1.5em rgba(0, 0, 0, 0.1),
      inset 0 0.125em 0.5em rgba(0, 0, 0, 0.15);
  }

  .b-example-vr {
    flex-shrink: 0;
    width: 1.5rem;
    height: 100vh;
  }

  .bi {
    vertical-align: -0.125em;
    fill: currentColor;
  }

  .nav-scroller {
    position: relative;
    z-index: 2;
    height: 2.75rem;
    overflow-y: hidden;
  }

  .nav-scroller .nav {
    display: flex;
    flex-wrap: nowrap;
    padding-bottom: 1rem;
    margin-top: -1px;
    overflow-x: auto;
    text-align: center;
    white-space: nowrap;
    -webkit-overflow-scrolling: touch;
  }
</style>
<div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
  <header class="mb-auto">
    <div>
      <a href="/">
        <h3 class="logo float-md-start mb-0">SeSAC-it</h3>
      </a>
      <nav class="nav nav-masthead justify-content-center float-md-end">
        <% if (isAuthenticated) { %>
        <!-- 로그인 상태일 때의 메뉴 -->
        <a class="nav-link fw-normal py-1 px-0" href="/mapMain">맛집 지도</a>
        <a class="nav-link fw-normal py-1 px-0" href="/boardMain">게시판</a>
        <a class="nav-link fw-normal py-1 px-0" href="/comment">댓글</a>
        <a class="nav-link fw-normal py-1 px-0" href="/mypage">내 프로필</a>
        <a class="nav-link fw-normal py-1 px-0" href="/logout">로그아웃</a>

        <% } else { %>
        <!-- 로그아웃 상태일 때의 메뉴 -->
        <a class="nav-link fw-normal py-1 px-0" href="/mapMain">맛집 지도</a>
        <a class="nav-link fw-normal py-1 px-0" href="/boardMain">게시판</a>
        <a class="nav-link fw-normal py-1 px-0" href="/signin">로그인</a>
        <a class="nav-link fw-normal py-1 px-0" href="/comment">댓글</a>
        <% } %>
      </nav>
    </div>
  </header>
</div>
